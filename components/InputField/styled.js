import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  inputContainer: {
    padding: 10,
    textAlign: 'right',
    width: 'calc(80% - 30px)'
  },
  icon: {
    paddingRight: 10,
  }
});